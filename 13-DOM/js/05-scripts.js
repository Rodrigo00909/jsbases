// QuerySelectorAll: Selecciona, a diferencia del query selector normal, retorna TODOS los elementos que cumplan con la condicion
const card = document.querySelectorAll('.card');
console.log(card);

// Si lo que buscamos no existe retorna un NodeList vacio