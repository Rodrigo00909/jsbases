// Cargar las variables: Para seleccionar el carrito por su id, una variable donde estarán cargadas los cursos(html)
// Una variable boton para vaciar el carrito, una variable lista de cursos que tendrá todos los cursos cargados para agregarlos
// Y una variable que tenga la lista de cursos

// Crear tambien el evento que esperara el click del usuario en 'agregar al carrito' para hacer todo el proceso de agregarlo
// Funciones: una tendra que verificar si tienen la clase agregar-carrito para poder agregarlos, y si lo tienen entonces
// se debera acceder al curso seleccionado, seleccionando el card completo del curso (parent element).
// Otra funcion debera leer los datos del curso seleccionado, el cual tendrá un objeto con todo lo que nos interesa del curso
// En la misma funcion, para que cada vez que se agrega un nuevo curso no pise el anterior debere hacer el spread operator
// aunque tambien se puede hacer con push(). Luego una funcion que recorrera el objeto de los carritos y lo ira agregando todo

// Variables: 
const cargarCursos = document.createElement('');

// Registrar eventos


// Funciones

// Muestra el carrito de compras en html
